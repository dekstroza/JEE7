apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: default
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 7881
    targetPort: 8080
  selector:
    app: users-service

