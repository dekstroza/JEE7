apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: customer-service-ingres
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: customer-service
    http:
      paths:
      - path: /customer_service/
        backend:
          serviceName: customer-service
          servicePort: 7777
